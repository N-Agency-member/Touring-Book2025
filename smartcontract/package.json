{
  "name": "trav-coin-token",
  "version": "1.0.0",
  "description": "Source code for the TRVT BEP-20 smart contract.",
  "main": "truffle-config.js",
  "scripts": {
    "compile": "truffle compile --all",
    "migrate": "truffle migrate --network testnet --reset --compile-all",
    "verify": "truffle run verify CoinSackToken --network testnet"
  },
  "author": "Sora",
  "license": "MIT",
  "devDependencies": {
    "@truffle/hdwallet-provider": "^1.5.0",
    "solc": "^0.8.7-fixed",
    "truffle": "^5.4.1",
    "truffle-plugin-verify": "^0.5.14"
  }
}
