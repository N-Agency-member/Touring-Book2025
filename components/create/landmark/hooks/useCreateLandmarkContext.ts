// Tools
import { useContext } from "react";
import { CreateLandmarkContext } from "../context";

// eslint-disable-next-line import/no-anonymous-default-export
export default () => useContext(CreateLandmarkContext);
