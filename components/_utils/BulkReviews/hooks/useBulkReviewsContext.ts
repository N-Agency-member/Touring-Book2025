// Tools
import { useContext } from "react";
import { BulkReviewsContext } from "../context";

// eslint-disable-next-line import/no-anonymous-default-export
export default () => useContext(BulkReviewsContext);
