// Tools
import { useContext } from "react";
import { CreateReviewContext } from "../createReviewContext";

// eslint-disable-next-line import/no-anonymous-default-export
export default () => useContext(CreateReviewContext);
